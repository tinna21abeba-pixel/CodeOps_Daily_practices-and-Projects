# Validated, Persistent Signup Form

A simple signup form that validates a user's name and Ethiopian phone number. It uses regex for validation, displays clear error messages, and saves valid entries persistently to your browser's local storage so that data remains across page reloads.

## How to Open

Open `index.html` directly in any modern web browser to view and use the form. No build step or local server is required.
